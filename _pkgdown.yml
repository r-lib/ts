url: https://r-lib.github.io/ts/

template:
  package: tidytemplate
  bootstrap: 5

development:
  mode: auto

navbar:
  structure:
    left: [about, reference]
    right: [news, search, github]
  components:
    about:
      text: About ts
      href: reference/about.html

reference:
- title: About ts
  desc: |
    * See ['About ts'](about.html) for an overview of ts.
    * If you want to edit a document start at `ts_tree_new()` and
      `ts_tree_select()`.
    * For more see the sections below.
  contents:
  - ts_list_parsers

- title: Create tree-sitter trees
  contents:
  - ts_tree_new

- title: Selection
  contents:
  - ts_tree_select

- title: Editing
  contents:
  - ts_tree_delete
  - ts_tree_format
  - ts_tree_insert
  - ts_tree_update

- title: Manipulation
  desc: |
    Another way to edit a tree-sitter tree is "in place", using the
    `ts_tre_select<-()` replacement function or the `[[<-` replacement
    operator.
  contents:
  - "ts_tree_select<-"
  - "`[[<-.ts_tree`"
  - ts_tree_deleted

- title: Formatting and printing
  contents:
  - print.ts_tree
  - format.ts_tree

- title: Unserialize
  contents:
  - "`[[.ts_tree`"
  - ts_tree_unserialize
  - ts_tree_write

- title: Exploring
  contents:
  - as.character.ts_tree
  - as.raw.ts_tree
  - "[.ts_tree"
  - ts_tree_ast
  - ts_tree_dom
  - ts_tree_query
  - ts_tree_sexpr

- title: Functions for tree-sitter parser packages
  desc: |
    These functions are intended to be used by packages that
    implement tree-sitter parsers and use the `ts` package as a backend.
    It is unlikely that you will need to use these functions directly.
  contents:
  - as_ts_caller_arg
  - ts_caller_arg
  - ts_caller_env
  - ts_check_named_arg
  - ts_cnd
  - ts_collapse
  - ts_parse_error_cnd
  - ts_tree_mark_selection1
  - ts_tree_select1
  - ts_tree_selected_nodes
  - ts_tree_selection

- title: internal
  contents:
  - about
